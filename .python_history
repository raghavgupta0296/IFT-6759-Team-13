import tensorflow
import tensorflow as tf
mnist = tf.keras.datasets.mnist
tf.__version__
(x_train, y_train), (x_test, y_test) = mnist.load_data()
exit()
import os
len(os.listdir("memory_chunks"))
import os
len(os.listdir('memory_chunks'))
l = len(os.listdir('memory_chunks'))
l*0.7
580+l*0.2
829-745
a = [1,2,3,4,5,5]
a[:3]
def g(x,y):
	return a[x:y]
g(1,2)
g(1,3)
g(1,)
g(1,None)
exit()
import tensorflow
exit()
